/*
Crie um procedimento que receba a quantidade de dinheiro em rais que uma    pessoa que vai viajar possui. Essa pessoa vai passar por vários países e 
precisar converter seu dinheiro em Dollar e em Euro e Iene.
Sabe-se que a cotação do (Dollar é R$ 4.00),do (Euro é R$ 4.25),e o (Iene é R$ 0.10). O procedimento devera fazer a leitura dos dados do Usuário e exibir o resultado das conversões diretamente,sem passagem de parâmetros.Desenvolva um
algorítimo que invoque o procedimento para realizar os cálculos...
*/

#include"cotacao"

void cotacao(void);

int main(int argc, char const *argv[])
{
    cotacao();
    return 0;
}
